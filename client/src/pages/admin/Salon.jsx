const Salon = () => {
  // TODO : front
  /*
    ? gestion des salons
    ? nombre personnes
    ? bouton pour supprimer un salon
    ? modification du nom du salon
  */
  return (
    <div className="salon container mx-auto px-4 flex flex-col justify-center items-center">
      <h1>Page Salon</h1>
    </div>
  );
};

export default Salon;
