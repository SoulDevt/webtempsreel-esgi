const ListeSalon = () => {
  // TODO : front
  /*
    ? liste des salons
    ? bouton pour créer un salon
    ? bouton pour supprimer un salon
    ? lien vers la gestion des salons
  */
  return (
    <div className="liste-salon container mx-auto px-4 flex flex-col justify-center items-center">
      <h1>Liste des salons</h1>
    </div>
  );
};

export default ListeSalon;
