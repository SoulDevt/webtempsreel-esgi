const Home = () => {
  // TODO: front
  /* 
    ? interface admin avce liens vers les autres pages
    ? bouton pour se mettre disponible pour le chat
    ? emettre notif
  */
  return (
    <div className="home-admin container mx-auto px-4 flex flex-col justify-center items-center">
      <h1>Interface admin</h1>
    </div>
  );
};

export default Home;
